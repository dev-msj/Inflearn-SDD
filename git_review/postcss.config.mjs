/**
 * Tailwind CSS v4 PostCSS 설정
 * v4부터 Tailwind는 별도 PostCSS 플러그인 패키지(@tailwindcss/postcss)로 분리되었다.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
